# 專案目標
家庭記帳與管理 App。核心功能：
1. **家庭資產管理**：記錄收支、帳戶餘額、淨資產追蹤
2. **家事提醒**：地產稅、報稅、牌照稅、HOA、車庫保養、車子保養、濾芯更換、冷氣保養等定期或一次性任務

# Next.js 讀取規則
優先參考現有程式碼。只在處理 routing、Server Actions、Proxy/Middleware、metadata、caching、PWA、deployment 等 Next.js 特定行為時，才讀 `node_modules/next/dist/docs/` 裡的相關指南。小型 UI、文案、樣式、本地重構不需要查文件。

# 確認優先原則
小型、明確、可還原的請求直接執行並回報變更。只有在多個目標衝突、大型產品決策、破壞性操作、正式部署、資料遷移、或不明確的取捨時，才詢問使用者確認。

# 修改、暫存與部署流程
1. 每次修改前先用 `git status --short` 確認目前工作區狀態，避免覆蓋使用者或其他代理已做的變更。
2. 手動修改檔案時一律沿用同一套方式：直接在原檔做最小範圍變更，使用 `apply_patch` 編輯，不另建臨時副本或用不同工具整檔覆寫。
3. 若同一需求需要多個檔案調整，集中在同一輪完成並保持變更範圍清楚；不要把無關檔案一起改進來。
4. 修改完成後先回報實際改了什麼，並依變更範圍執行合適檢查，例如 `npm run lint`、`npx tsc --noEmit` 或 `npm run build`。
5. 每次修改完成後，一律主動詢問使用者是否要部署到 `https://family-app-ruddy-one.vercel.app`。
6. 只有在使用者明確同意後，才執行部署到該網址。
