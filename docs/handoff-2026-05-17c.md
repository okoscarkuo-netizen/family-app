# 交接文件 — 2026-05-17（晚場）

## 本次完成的工作

### 操作介面優化 — 已完成

**設計決策：**
- 導航：底部 Tab Bar（5 欄）
- 視覺風格：Neobrutalist（帳戶、帳本、首頁）
- 首頁：三張摘要卡片取代舊 Dashboard
- Tab Bar 風格：與舊 Dashboard nav 一致（白底毛玻璃 + 藍色陰影 + 粉紅 active）

**新建檔案：**
```
components/BottomNav.tsx              ← 共用底部 Tab Bar（5 欄，含「記一筆」）
app/_components/NetWorthCard.tsx      ← 淨資產摘要卡
app/_components/MonthlySummaryCard.tsx ← 本月收支摘要卡
app/_components/UpcomingRemindersCard.tsx ← 即將到期卡（靜態空狀態）
```

**修改檔案：**
```
app/page.tsx              ← 首頁改為 Server Component（三張摘要卡片）
app/accounts/page.tsx     ← 加 BottomNav + pb-20
app/accounts/[id]/page.tsx ← 加 BottomNav + pb-20
app/ledger/page.tsx       ← Neobrutalist 風格 + BottomNav
app/reminders/page.tsx    ← 加 BottomNav（Dashboard wrapper 保留不動）
```

**設計文件：** `docs/superpowers/specs/2026-05-17-ui-optimization-design.md`
**實作計畫：** `docs/superpowers/plans/2026-05-17-ui-optimization.md`

**Git commits（本次新增）：**
```
e929755 style: remove emoji icons from BottomNav tabs
e9afea8 feat: add 記一筆 center button to BottomNav
73c8ff2 style: BottomNav matches Dashboard nav (white/blur, blue shadow, pink active)
2db8bd5 feat: add BottomNav to reminders page
2497ace fix: clamp savingRatio to [0,1] when expense exceeds income
0ae69a5 feat: rewrite home page as summary Server Component
0a6cea6 feat: add home summary card components
75e0095 feat: apply neobrutalist style and add BottomNav to ledger page
82235a3 feat: add BottomNav to accounts pages
b500cc0 feat: add BottomNav component
```

---

## 現有架構（更新版）

| 路徑 | 說明 | 資料來源 | Tab Bar |
|------|------|---------|---------|
| `/` | 首頁摘要（三張卡片） | Supabase | ✅ |
| `/accounts` | 帳戶管理 | Supabase | ✅ |
| `/accounts/[id]` | 帳戶明細 | Supabase | ✅ |
| `/ledger` | 帳本 | Supabase | ✅ |
| `/ledger/new` | 新增交易 | Supabase | — |
| `/reminders` | 提醒（舊 Dashboard wrapper）| localStorage | ✅ |

**Tab Bar 5 欄：** 首頁 ｜ 帳戶 ｜ **記一筆** ｜ 帳本 ｜ 提醒

**Tab Bar 樣式：**
- 白底毛玻璃 `bg-white/95 backdrop-blur`
- 頂部藍色陰影 `shadow-[0_-10px_0_#00c2ff]`
- Active tab：粉紅底白字 `bg-[#ff3d9a] text-white`
- 「記一筆」：黃底黑邊框粉紅陰影，連結到 `/ledger/new`

---

## 首頁卡片說明

| 卡片 | 底色 | 資料來源 | 備註 |
|------|------|---------|------|
| 淨資產 | `#00c2ff` 藍 | `getAccounts()` | 計算 asset - liability |
| 本月收支 | 白 | `getTransactions({ year, month })` | 現在 $0，等資料匯入 |
| 即將到期 | `#fff45f` 黃 | 靜態空狀態 | 提醒系統建好後接 DB |

---

## 已知問題

1. **提醒頁雙層導航**：`/reminders` 底部同時有舊 Dashboard 的 nav + 新 BottomNav，視覺上稍微重疊。等提醒系統重寫時一起解決。
2. **首頁本月收支顯示 $0**：資料匯入完成後自動有資料。
3. **提醒卡永遠顯示「尚無待辦提醒」**：提醒系統建好後接 DB。

---

## 下一步建議（優先順序）

### 主要任務
- [ ] **D. AndroMoney 歷史資料匯入**（16,353 筆，先決定 3,590 筆無帳戶資料的處理方式）
- [ ] **C. 提醒系統**（帳單/維護到期提醒，資料表已在 schema.sql）
- [ ] **A. 帳本月結總覽**（資料匯入後才有意義）

### 技術債
- [ ] 提醒頁雙層導航問題（等提醒系統重寫時一起處理）

---

## 重要架構提醒

1. **Passcode Auth**：cookie 驗證，沒有 Supabase Auth
2. **Admin Client**：`createAdminClient()` 可能回傳 null，所有呼叫要先 null check
3. **family_accounts.id 是 text**，含特殊字元，URL 要用 `encodeURIComponent`
4. **`/api/accounts` REST route 保留**：`dashboard.tsx` 仍在使用
5. **balance 欄位**：目前存起始餘額（全部為 0），待資料匯入後更新

## 環境
- Dev server：`npm run dev`（http://localhost:3000）
- Production：https://family-app-ruddy-one.vercel.app
- GitHub：`git@github.com:okoscarkuo-netizen/family-app.git`
