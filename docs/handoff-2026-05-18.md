# 交接文件 — 2026-05-18

## 專案目標
家庭記帳與管理 App。核心功能是家庭資產管理、記帳、以及家事/帳單/保養提醒。

## 目前狀態總結
- Production 已上線：`https://family-app-ruddy-one.vercel.app`
- 最新 production deployment：`dpl_AqQjHaRPtPotArQmRAPUeWGYS922`
- 最新 deployment inspector：`https://vercel.com/okoscarkuo-netizens-projects/family-app/AqQjHaRPtPotArQmRAPUeWGYS922`
- 已完成全站分頁風格統一，主要頁面共用同一套頁面殼、卡片與按鈕語言
- `lint` 與 `build` 都已通過，production deployment 成功
- 登入已切到 Supabase Email + 密碼
- 兩個帳號都已建立且可登入
- 舊的 passcode 相關檔案已刪除：`proxy.ts.bak`、`lib/auth/access.ts`
- Home / Dashboard 的登入文案已改成 Supabase 帳號密碼登入
- 已用 Vercel 直接重新部署，production alias 已更新

## Supabase 帳號狀態
- `ironoscarkuo@gmail.com`：可登入
- `ok.oscar.kuo@gmail.com`：可登入
- 兩個帳號的密碼已重設並驗證成功
- 注意：不要再用舊通行碼思維測試登入，現在是標準 Supabase Auth

## 已完成的近期變更
### 視覺統一
- 新增共用頁面殼與樣式 token：`components/PageShell.tsx`
- `app/accounts/*`、`app/ledger/*`、`app/login/page.tsx`、`app/reset-password/page.tsx` 都改成同一套外殼
- 全域背景改成同一種柔和底圖與紙感網格，減少每頁落差

### 認證
- `app/actions/auth.ts`：使用 `supabase.auth.signInWithPassword` / `signOut`
- `middleware.ts`：改成 Supabase session 驗證
- `app/login/page.tsx`：顯示 Email + 密碼欄位
- `app/auth/callback/route.ts`：保留 Supabase callback 流程

### Dashboard 雲端快照
- `app/dashboard.tsx` 的 `categories`、`todos`、`bills`、`maintenance`、`entries` 已開始同步到 Supabase 的 `household_dashboard_state`
- 首頁與提醒頁現在會讀取真實 session email 顯示登入者
- localStorage 仍保留作為離線快取與回退來源

### UI / 文案
- `app/dashboard.tsx`：把「私人通行碼保護」改成「Supabase 帳號密碼登入」
- `app/page.tsx`：首頁維持舊 Dashboard 風格 + 摘要卡

### 清理
- 已移除舊 passcode 備份與舊 access helper

## 重要提醒
1. 目前 `app/dashboard.tsx` 仍是大型舊 Dashboard 核心檔，首頁 / 提醒還是會依賴它。
2. `createAdminClient()` 仍是所有 DB 讀寫的主力，service role key 沒有 RLS 限制。
3. `public/sw.js` 會快取 `/` 和 `/login`，如果使用者看到舊頁面，先懷疑 PWA/service worker cache。
4. 部署時 Vercel build 會出現 `middleware` 已 deprecated 的提醒。這不是當下故障，但後續可評估改成 `proxy` 命名。
5. repo 裡還有一些舊文件仍寫著 passcode / private gate，這些文件目前是歷史紀錄，不代表實際登入機制。
6. `git status` 目前還有很多未追蹤檔案，先視為既有工作樹雜訊，不要在沒有確認前清掉或重建。

## 建議下一步
- 把剩下的 passcode 舊文案與舊文件描述逐步清掉，避免誤解
- 開始把 Dashboard 的本機狀態接到 Supabase
- 先處理：
  - 記一筆 / 待辦 / 帳單 / 維護提醒的雲端同步
  - 舊 Dashboard 內部狀態拆分
- 若要減少 production 舊快取困擾，可以考慮更新 service worker cache version

## 常用檢查
- `/`：應該進首頁摘要，不該再顯示通行碼字樣
- `/login`：應顯示 Email / 密碼登入
- `npx vercel inspect https://family-lkkb3eb79-okoscarkuo-netizens-projects.vercel.app`：確認最新 production deployment 狀態
- `npm run lint`：目前可通過
