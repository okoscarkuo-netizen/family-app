# 交接文件 — 2026-05-24

## Project Goal
家庭記帳與管理 App。核心功能是家庭資產管理、記帳、帳戶餘額/淨資產追蹤，以及家事、帳單、保養提醒。

## Current State
- Repo path: `/Users/hankuo/Documents/AI_Workspace/1_Projects/Family_App`
- 工作樹很髒，且包含大量既有未提交變更，請不要用 destructive git 指令清理。
- 目前已確認：`family_transactions` 在 Supabase 真的為 0 筆，`family_accounts` 仍有 105 筆。
- `family_ledger_entries` 也為 0 筆。
- 這不是前端顯示問題，而是資料庫內交易歷史真的空了。

## What Changed
本輪主要是針對「AndroMoney 交易如何安全重匯」做準備。

### 新增檔案
- `supabase/migrations/20260524003000_reimport_family_transactions_from_json.sql`
  - 新增一個原子化 RPC：`public.reimport_family_transactions_from_json(jsonb)`
  - 目標是把交易匯入包成單一交易，避免半套寫入。
- `imports/andromoney/scripts/reimport-family-transactions-safe.mjs`
  - 新的安全重匯腳本，先 dry-run，再 `--apply`。
  - 會讀取原始 AndroMoney CSV，解析帳戶 / 分類 / 商家，並預估每個帳戶的 delta。
- `imports/andromoney/README.md`
  - 補上安全重匯操作說明。

## What Was Verified
- `git status --short` 已確認，工作樹本來就有大量既有變更。
- `node --check imports/andromoney/scripts/reimport-family-transactions-safe.mjs` 通過。
- `npx eslint --no-ignore --no-warn-ignored imports/andromoney/scripts/reimport-family-transactions-safe.mjs` 通過。
- 乾跑結果：
  - 原始列數：16,320
  - 可匯入交易：4,328
  - 其中非零交易：4,135
  - 零金額交易：193
  - 跳過：system=145、blank=9,752、same-account-transfer=3、missing-account=2,092
- 乾跑時也確認：
  - 目前 `family_accounts` 還沒有 `opening_balance` 欄位
  - 因此安全流程已改成不依賴這個欄位

## What Is Still Open
最重要的阻礙是：**還沒有辦法真的把 migration / RPC 套到 Supabase production DB**。

已找到的線索：
- `.temp/pooler-url` 有 pooler host：
  - `postgresql://postgres.ampmemzawmqwcplmclze@aws-1-us-west-1.pooler.supabase.com:5432/postgres`
- 但目前仍缺：
  - DB password / connection string
  - 或可用的 Supabase CLI access token

因此現在卡在：
1. `supabase/migrations/20260524003000_reimport_family_transactions_from_json.sql` 尚未進到 production DB
2. `imports/andromoney/scripts/reimport-family-transactions-safe.mjs --apply` 也還不能真正執行

## Risks / Gotchas
- 交易重匯如果不是原子操作，很容易再次出現「帳戶在、流水空」的半套狀態。
- `family_accounts.balance` 是現值快照，不能把歷史流水直接加到現有餘額上，否則會重複計算。
- 原始 AndroMoney 資料有大量沒有辦法直接對上的項目，安全腳本目前會跳過非零且找不到帳戶對應的列。
- 目前 dry-run 仍有 2,092 筆因缺帳戶被跳過，主要是命名變體與部分原始資料對照問題。
- 本次對帳中看到的主要缺口名稱包括：
  - `家庭口袋現金`
  - `翰融Buffer`
  - `代刷墊`
  - `US現金`
  - `T-嘉信US`
  - `爸爸負債帳戶`
  - `立宏代刷墊`

## Verification / Deployment
- 沒有做部署。
- 沒有真的把交易寫回 Supabase production DB。
- 目前只有 dry-run 驗證完成。

## Next Steps
1. 先取得可用的 Supabase DB access 方式。
   - 優先：DB password / connection string
   - 次選：Supabase CLI access token
2. 將 `supabase/migrations/20260524003000_reimport_family_transactions_from_json.sql` 套到 production DB。
3. 跑：
   - `node imports/andromoney/scripts/reimport-family-transactions-safe.mjs --apply`
4. 匯入完成後立刻驗證：
   - `family_transactions` 是否回到預期筆數
   - `family_ledger_entries` 是否生成
   - `family_accounts.balance` 是否仍維持目前快照
5. 若 apply 成功，再補一輪帳戶 / 交易頁面的 sanity check。
